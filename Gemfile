source "https://rubygems.org"

# Build tooling only. fastlane uploads store listing text and screenshots that
# were generated from reviewed sources in docs/; it is never bundled into the
# app and has no bearing on what ships to users.
gem "fastlane", "~> 2.228"
