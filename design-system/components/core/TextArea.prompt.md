The multi-line field — system prompts, custom instructions, restore passphrases.

```jsx
<TextArea value={instruction} onChange={setInstruction} placeholder="Answer plainly." />
```

The box is fixed at 128pt and scrolls; it does not grow with content.
