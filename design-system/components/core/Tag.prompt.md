A selectable chip, used for the settings filter row (All, First Run, Providers, STT, TTS…).

```jsx
<Tag selected={filter === "stt"} onChange={() => setFilter("stt")}>STT</Tag>
```

Selection is a fill plus an accent border, not colour alone.
