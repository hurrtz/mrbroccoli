The paragraph directly under an \`IntroTitle\`.

\`\`\`jsx
<IntroBody>One thing, and it is a download rather than a decision. Everything else is optional and can wait.</IntroBody>
\`\`\`

Two or three sentences at most. It is centred to match the heading, which only works at short lengths — anything longer belongs in the step body itself, left-aligned.
