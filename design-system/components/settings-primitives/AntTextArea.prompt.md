The settings-page long-text field — system prompt, custom instructions.

```jsx
<AntTextArea value={prompt} placeholder="Answer plainly." onChange={setPrompt} />
```

Identical geometry to `TextArea`; use this one inside a settings card so the defaults stay consistent.
