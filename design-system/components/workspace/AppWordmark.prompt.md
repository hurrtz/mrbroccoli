The app title in the top bar, set in Unica One at 26/32 with -0.25 tracking.

```jsx
<AppWordmark name="Mr Broccoli" />
```

There is no logo lockup in the product: the wordmark is the brand mark on screen. `compact` gives the pill treatment used when vertical space is short.
