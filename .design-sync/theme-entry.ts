// Bundle entry for gen-tokens.mjs — the two theme modules it needs, together.
export { lightColors, darkColors } from "../src/theme/colors";
export { fonts, textStyles } from "../src/theme/typography";
